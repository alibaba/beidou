sudo: false
git:
  depth: 10
language: node_js
node_js:
  - '6'
  - '8'
  - '9'
cache:
  yarn: true
  directories:
    - "node_modules"
before_install:
  # Remove examples directories, avoid install dependencies
  - rm -rf ./examples/
install:
  - yarn
  - yarn global add codecov
script:
  - yarn run ci
after_script:
  - codecov
